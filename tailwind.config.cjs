const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		fontFamily: {
			sans: ["Plus Jakarta Sans", "sans-serif"],
		},
		extend: {}
	},

	plugins: []
};

module.exports = config;
